opensearch:
  url: "http://cloud-1:9200"
  index_name: "flibusta_merged_index"
  debug: true

datamanager:
  protocol: "tcp"
  host: "localhost"
  port: 50051
  debug: false

orchestrator:
  protocol: "tcp"
  host: "localhost"
  port: 50053
  debug: false

web_adapter:
  protocol: "tcp"
  host: "localhost"
  port: 50080
  debug: false

cli:
  debug: false

lisp_converter:
  protocol: "tcp"
  host: "localhost"
  port: 50052

metrics:
  port: 50090
