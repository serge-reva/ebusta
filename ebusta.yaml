opensearch:
  url: "http://cloud-1:9200"
  index_name: "flibusta_merged_index"
  debug: true

datamanager:
  protocol: "tcp"
  host: "localhost"
  port: 50051

orchestrator:
  protocol: "tcp"
  host: "localhost"
  port: 50053

web_adapter:
  protocol: "tcp"
  host: "localhost"
  port: 50080

cli:

lisp_converter:
  protocol: "tcp"
  host: "localhost"
  port: 50052


dsl_scala:
  protocol: "tcp"
  host: "localhost"
  port: 50054


query_builder:
  protocol: "tcp"
  host: "localhost"
  port: 50055


metrics:
  port: 50090
