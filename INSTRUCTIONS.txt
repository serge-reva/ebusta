=== КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ ИИ ===

1. НИКОГДА не удаляй и не меняй поля в proto файлах
2. ВСЕГДА смотри git diff перед изменениями
3. ВСЕГДА спрашивай подтверждение перед cat > file
4. ИСПОЛЬЗУЙ только git checkout для отката
5. НЕ используй sed для proto файлов

Источник истины:
- api/proto/v1/library.proto - ОСНОВНОЙ контракт
- lisp-converter/*.lisp - логика DSL

Перед ЛЮБЫМ изменением:
1. git diff <file>
2. Спросить подтверждение
3. Создать резервную копию
