{
  "script": {
    "lang": "mustache",
    "source": {
      "query": {
        "match": {
          "authors": {
            "query": "{{author}}",
            "operator": "and"
          }
        }
      },
      "size": "{{size}}",
      "from": "{{from}}",
      "_source": ["title", "authors", "fileInfo.container", "fileInfo.filename"],
      "track_total_hits": false
    }
  }
}
