{
  "script": {
    "lang": "mustache",
    "source": {
      "query": {
        "match": {
          "title": {
            "query": "{{query}}",
            "operator": "and"
          }
        }
      },
      "from": "{{from}}",
      "size": "{{size}}"
    }
  }
}
