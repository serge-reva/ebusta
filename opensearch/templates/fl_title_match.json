{
  "script": {
    "lang": "mustache",
    "source": {
      "query": {
        "match": {
          "title": {
            "query": "{{q}}",
            "operator": "and"
          }
        }
      },
      "from": "{{from}}",
      "size": "{{size}}"
    }
  }
}
