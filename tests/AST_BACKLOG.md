# AST Backlog (DSL / CanonicalForm)

Эти пункты зафиксированы по фактическому поведению DSL через `orch.log` (строки `🎼 DSL CanonicalForm:`).

## 1) Скобки "прилипают" к токенам (BUG парсера)
Запрос:
- `(author:Кинг AND title:Туман) OR id:101`

Фактическое поведение (пример из лога):
- `(:OR (:AND (:FIELD "author" "Кинг") (:FIELD "title" "Туман)")) (:FIELD "id" "101"))`
  - закрывающая `)` попала внутрь значения `"Туман)"`, плюс возможен разрыв строки.

Ожидаемое (целевое):
- `(:OR (:AND (:FIELD "author" "Кинг") (:FIELD "title" "Туман")) (:FIELD "id" "101"))`

Статус:
- В функциональном наборе НЕ проверяем (это зона DSL), кейс исключён/заменён.

## 2) "Неверный синтаксис" не считается ошибкой (текущее поведение)
Запрос:
- `author:Кинг)`

Фактическое:
- `(:FIELD "author" "Кинг)")` (т.е. `)` считается частью значения)

Варианты решения (решить на стороне DSL):
- A) считать это синтаксической ошибкой (`DSL Error`)
- B) оставить как literal, но тогда это закрепляется как контракт

Статус:
- В функциональных AST тестах закреплено как текущее поведение (literal).

## 3) Приоритет AND/OR (текущее поведение)
Запрос:
- `author:Кинг AND title:Туман OR id:101`

Фактическое:
- `(:OR (:AND ...) (:FIELD ...))`

Статус:
- Зафиксировано функциональными тестами как контракт.
